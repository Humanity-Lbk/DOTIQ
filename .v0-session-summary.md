# v0 Session Summary
> Auto-generated PR descriptions from v0 sessions

---

## Session: v0/treywoodward-48476fff
**Date:** 2026-04-15 | **Branch:** main → v0/treywoodward-48476fff

### Summary
Added comprehensive admin features including a request/ticket system, role-based access control, and improved change log functionality.

### Features Added

#### 1. Role-Based Access Control
- Added `role` column to profiles table: `user` (default), `admin`, `super_admin`
- Set `trey@gethumanity.ai` as `super_admin` 
- Super admins have access to internal views and admin dashboards

#### 2. Request/Ticket System (`/requests`)
- Admins can submit feature requests, change requests, bug reports, or error tickets
- Image/screenshot upload support via Vercel Blob
- Super admins can view all requests with status tracking (pending → in_progress → resolved → closed)
- RLS policies for data security
- API routes for creating, updating, and managing requests

#### 3. Improved Time Logging
- Changed calculation from fixed 1-hour per commit to delta between consecutive commits
- Delta capped at 3 hours max per gap (prevents overnight inflation of 8+ hours)
- Minimum 0.25 hours, rounded to nearest quarter hour
- Auto-generates user-friendly titles (strips prefixes like `feat:`, `fix:`)
- Auto-generates descriptions based on commit categories
- Timestamps display date + time for each entry

#### 4. Enhanced Admin Menu
- Avatar dropdown now includes role-specific sections
- Admin section shows: "Requests & Tickets" + "Change Log" links
- Super admins see both external and internal change log views
- `/client-updates?view=internal` for internal-only features

### Bug Fixes
- Fixed avatar dropdown hover not sticking (replaced CSS `group-hover` with click-toggle state)
- Fixed GitHub API 404 auth error (changed header from `Bearer` to `token` format)
- Cleared old time log entries and re-seeded with proper calculations

### Database Changes
```sql
-- New tables/columns:
ALTER TABLE profiles ADD COLUMN role TEXT DEFAULT 'user';
CREATE TABLE requests (type, title, description, attachments, status, ...);
ALTER TABLE time_log ADD COLUMN title TEXT, description TEXT;
```

### API Endpoints Added
- `POST /api/requests` - Create new request/ticket
- `GET /api/requests` - List requests (filtered by role)
- `PATCH /api/requests/:id` - Update request status (super_admin only)
- `POST /api/upload` - Upload images via Vercel Blob

### Files Modified
- `app/api/client-updates/route.ts` - Time calculation + title generation
- `app/api/requests/route.ts` - NEW: Request management
- `app/api/upload/route.ts` - NEW: File uploads
- `app/client-updates/page.tsx` - View mode logic + role checks
- `app/requests/page.tsx` - NEW: Request submission UI
- `components/header.tsx` - Admin menu + role-based nav
- Database migrations for role + requests table

### Testing Notes
- Re-seed from GitHub to populate time log with proper delta calculations
- Test request submission with image uploads
- Verify super_admin sees internal view and all requests
- Verify admin sees only external view and own requests

---

## How to Use This File

1. **Copy for PR Description** - Use the relevant section above when creating a PR
2. **Update Format** - Add new sessions in reverse chronological order (newest first)
3. **Auto-Update** - At end of each v0 session, I'll append a new section here

## Quick Copy Template
```markdown
### Summary
[Copy from session summary above]

### Changes
- Database: ...
- Features: ...
- Bug Fixes: ...
- Files: ...
```
